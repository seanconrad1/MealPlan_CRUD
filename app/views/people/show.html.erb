<h1><%= @person.name %>'s page</h1>

<p>My meals</p>
<% @person.meals.each do |meal| %>
  - <%= meal.name %> | Healthy? <%= meal.healthy %><br>
  <% end %><br>

<%= link_to "Edit", edit_person_path(@person) %>

<%= link_to 'Delete', person_path(@person), method: :delete, data: { confirm: 'Are you sure?' } %><br>

<%= link_to "Add new meal", new_meal_path %>

<%= link_to "All people", people_path %>
